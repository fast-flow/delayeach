{
  "name": "delayeach",
  "version": "0.1.0",
  "main": "index.js",
  "description": "delay each delayEach(arr, iterateFn, finishFn)",
  "keywords": "delay each, task each",
  "devDependencies": {
    "expect.js": "^0.3.1"
  }
}
