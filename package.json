{
  "name": "delayeach",
  "version": "0.1.2",
  "main": "index.js",
  "description": "delay each `delayEach(arr, iterateFn(item, index, next, finish), finishFn)`",
  "keywords": "delay each, task each",
  "devDependencies": {
    "expect.js": "^0.3.1"
  }
}
